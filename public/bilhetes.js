document.addEventListener('DOMContentLoaded',()=>{const g=document.querySelector('#ticketGrid'),f=document.querySelector('#ticketFilters'),m=document.querySelector('#analysisModal');let ts=[];const e=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const ml=v=>({'Goals Over/Under':'Total de gols','Both Teams Score':'Ambas marcam','Match Winner':'Resultado da partida','Double Chance':'Dupla chance','Team Goals':'Gols da equipe','Corners':'Escanteios','Total Corners':'Total de escanteios','Team Corners':'Escanteios da equipe','Cards':'Cartões','Total Cards':'Total de cartões','Team Cards':'Cartões da equipe','Shots':'Finalizações','Shots on Goal':'Finalizações no gol','Goalkeeper Saves':'Defesas do goleiro','First Half Goals':'Gols no 1º tempo'}[v]||v||'');const sl=v=>String(v??'').replace(/^Over\s+([0-9.]+)/i,'Mais de $1').replace(/^Under\s+([0-9.]+)/i,'Menos de $1').replace(/^Yes$/i,'Sim').replace(/^No$/i,'Não');const tl=v=>({'CHAMPIONS_MULTIPLA':'Champions — Múltipla','BILHETE_DO_DIA':'Bilhete do Dia','SUL_AMERICANA':'Sul-Americana','BINGO_CHAMPIONS':'Bingo Champions','BINGO_LIBERTADORES':'Bingo Libertadores','CONTINENTAL_DO_DIA':'Continental do Dia','CONSERVADOR':'Conservador','OUSADO':'Ousado','BINGO':'Bingo','CHAMPIONS':'Champions'}[v]||String(v??'').replaceAll('_',' '));const n=t=>({...t,totalOdd:t.totalOdd??t.total_odd,combinationType:t.combinationType??t.analysis?.combinationType,combinedOddType:t.combinedOddType??t.analysis?.combinedOddType,selections:(t.selections||[]).map(s=>({...s,displayMatch:s.displayMatch||`${s.homeTeam?.name||''} x ${s.awayTeam?.name||''}`.trim(),displaySelection:s.displaySelection||s.selectionDisplay,bookmakerName:s.bookmakerName||s.name}))});const renderAnalysis=t=>{const ev=t.analysis?.evidence||[];const blocks=(t.selections||[]).map(s=>{const row=ev.find(x=>Number(x.fixtureId)===Number(s.fixtureId));const q=row?.evidence?.[s.marketKey]?.evidence||row?.evidence?.[s.marketKey];if(!q)return `<div class="analysis-block"><strong>${e(s.displayMatch)}</strong><span>${e(sl(s.displaySelection))}</span><small>Detalhamento estatístico indisponível para esta seleção.</small></div>`;return `<div class="analysis-block"><strong>${e(s.displayMatch)}</strong><span>${e(sl(s.displaySelection))}</span><b>${Number(q.value)}%</b><small>${Number(q.hits)} de ${Number(q.total)} jogos analisados</small></div>`}).join("");document.querySelector(".analysis-placeholder").innerHTML=blocks||"<div class=analysis-block><small>Detalhamento estatístico indisponível para este bilhete.</small></div>"};const card=t=>`<article class="ticket-card"><div class="ticket-head"><div class="ticket-type">${e(tl(t.type))}<small>${e(t.description)}</small></div><span class="ticket-status">${t.status==='OPEN'?'ABERTO':e(t.status)}</span></div><div class="ticket-picks">${t.selections.map(s=>`<div class="pick"><span>${e(s.displayMatch)}<br><small>${e(ml(s.market||s.marketName))} · ${e(sl(s.displaySelection))}</small></span><strong>${Number(s.odd).toFixed(2)}</strong></div>`).join('')}</div><div class="ticket-foot"><span><small>ODD TOTAL · ${t.selections.length} ${t.selections.length===1?'seleção':'seleções'}${t.combinationType==='SAME_FIXTURE'?' · Mesmo jogo':''}${t.combinedOddType==='CALCULATED'?' · Odd combinada calculada':''}</small><strong>${Number(t.totalOdd).toFixed(2)}</strong></span><small>${e(t.date)}</small></div><div class="ticket-actions"><button data-analysis="${e(t.id)}">Ver análise</button></div></article>`;const today=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());const todays=()=>ts.filter(t=>t.date===today());const render=x=>{const day=todays();g.innerHTML=day.filter(t=>x==='Todos'||(x==='Aberto'&&t.status==='OPEN')||t.status===x).map(card).join('')||'<div class="empty-state">Nenhum bilhete encontrado para hoje.</div>'};const hist=()=>{document.querySelector('#historyTable').innerHTML=ts.filter(t=>t.date!==today()).map(t=>`<div class="history-row"><span>${e(t.date)}</span><strong>${e(tl(t.type))}</strong><span>${e(t.competition)}</span><span>${t.selections.length} seleções</span><span>${Number(t.totalOdd).toFixed(2)}</span><span>${t.status==='OPEN'?'ABERTO':e(t.status)}</span><a href="#ticketGrid">Ver</a></div>`).join('')};const upd=()=>{const day=todays();const count=(arr,s)=>arr.filter(t=>t.status===s).length;const dayValues=[day.length,count(day,'OPEN'),count(day,'GREEN'),count(day,'RED')];document.querySelectorAll('.ticket-summary article strong').forEach((x,i)=>x.textContent=dayValues[i]);document.querySelectorAll('.ticket-filters b').forEach((x,i)=>x.textContent=dayValues[i]);const allValues=[ts.length,count(ts,'OPEN'),count(ts,'GREEN'),count(ts,'RED')];document.querySelectorAll('.mini-stats b').forEach((x,i)=>x.textContent=allValues[i]);const finished=allValues[2]+allValues[3];const rate=finished?allValues[2]/finished*100:null;const roi=document.querySelector('.roi strong');if(roi)roi.textContent=rate===null?'—':`${Number.isInteger(rate)?rate.toFixed(0):rate.toFixed(1)}%`;const roiSmall=document.querySelector('.roi small');if(roiSmall)roiSmall.textContent=finished?`Taxa de acerto · ${allValues[2]} acerto${allValues[2]===1?'':'s'} em ${finished} bilhete${finished===1?'':'s'} finalizado${finished===1?'':'s'}`:'Taxa de acerto dos bilhetes';const round=document.querySelector('.round-note strong');if(round){const [y,m,d]=today().split('-');round.textContent=`${d}/${m}/${y}`}};f.addEventListener('click',x=>{const b=x.target.closest('button');if(b){f.querySelectorAll('button').forEach(y=>y.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)}});g.addEventListener('click',async x=>{const b=x.target.closest('[data-analysis]');if(b){const t=n((await fetch('/api/bilhetes/'+encodeURIComponent(b.dataset.analysis)).then(r=>r.json())).ticket||{});document.querySelector('#modalTitle').textContent=tl(t.title);renderAnalysis(t);m.hidden=false}});const close=()=>m.hidden=true;document.querySelector('#closeAnalysis').addEventListener('click',close);m.addEventListener('click',x=>{if(x.target===m)close()});document.addEventListener('keydown',x=>{if(x.key==='Escape'&&!m.hidden)close()});fetch('/api/bilhetes').then(r=>r.json()).then(p=>{ts=(p.tickets||[]).map(n);upd();render('Todos');hist();const z=document.querySelector('.history-section .section-heading>span');if(z&&ts.every(t=>t.source==='real'))z.textContent='REAL'}).catch(()=>g.innerHTML='<div class="empty-state">Não foi possível carregar os bilhetes persistidos.</div>')});
